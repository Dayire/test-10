# test-10

## Kafka in Motion

Open `kafka-animation.html` in a browser: an animated, step-by-step explanation of Apache Kafka (producers, topics, partitions, offsets, consumer groups, retention, brokers) with play/pause, step, and speed controls. No build step, no dependencies.
